# local development overrides
# binds internal services to all interfaces for host access
services:
  postgres:
    ports:
      - "0.0.0.0:5432:5432"

  minio:
    ports:
      - "0.0.0.0:9000:9000"
      - "0.0.0.0:9001:9001"
